/* theme.ts */
import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: 'var(--font-quick-sand)',
    body: 'var(--font-quick-sand)',
  },
});

export default theme
